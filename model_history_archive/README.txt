This folder is where you can store finished model and history files.

It is recommended to manually copy them over from the model_history_out
directory when training is complete in case of accidental overwrite.

Preferably store each run in a separate sub-directory.
